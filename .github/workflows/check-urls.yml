name: Check Provider URLs

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # ✅ अभी से Node 24 use होगा

jobs:
  check-urls:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5   # ✅ updated
      
      - name: Set up Node.js
        uses: actions/setup-node@v5   # ✅ updated
        with:
          node-version: '20'   # ⚠️ stable LTS use करो (25 avoid करो)
      
      - name: Install dependencies
        run: npm install axios

      - name: Run URL checker
        run: node .github/scripts/url-checker.js
        
      - name: Commit changes if any
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add urls.json
          
          if [[ $(git status --porcelain urls.json) ]]; then
            echo "Found changes in urls.json, committing..."
            git commit -m "Update provider URLs [skip ci]"
            git push
          else
            echo "No changes detected in urls.json"
          fi
